<% layout('layout') %>


<div class="container">
    <br><br>
    
   <div class="banner-title text-center reg_msg">
        <h2 class="fancy"><span>Reset Senha</span></h2>
   </div><br>

    <div class="row signup">
        
        <% if(hasErrors) {%>
            <% for(var i = 0; i < messages.length; i++) {%>
                <div class="alert alert-danger">
                   <a href="#" class="close" data-dismiss="alert" aria-label="close">
                       &times;
                   </a>
                   <%= messages[i] %>
                    
                </div>
            <% } %>
        <% } %>
        
        <% if(noErrors) {%>
            <div class="alert alert-success">
               <a href="#" class="close" data-dismiss="alert" aria-label="close">
                   &times;
               </a>
               <%= success %>

            </div>
        <% } %>

        <form method="POST">
            <div class="form-group input_group">
                <input id="password" class="form-control" name="password" type="password" autocomplete="on" placeholder="Senha" style="margin-bottom:20px;">
            </div>

            <div class="form-group input_group">
                <input id="cpassword" class="form-control" name="cpassword" type="password" autocomplete="on" placeholder="Confirme sua Senha" style="margin-bottom:20px;">
            </div>

            <div class="form-group" id="submit">
                <input type="submit" value="Alterar Senha" class="form-control btn btn-primary" name="reset" style="">
            </div>
        </form>

        <div>
            <h4>Sua senha foi alterada? <a href="/login">Entre</a></h4>
        </div>

    </div>
</div>
